{
    "name": "ROS2 Humble Dev",
    // 告诉 VS Code 附加到现有容器
    "dockerFile": "none",
    // 关键：指定容器内的工作区文件夹
    "workspaceFolder": "/root/ros2_ws",
    // 确保用户正确
    "remoteUser": "root",
    // 可选：容器附加后自动执行的命令
    "postAttachCommand": "echo 'Welcome to ROS2 Humble Dev Container'"
}